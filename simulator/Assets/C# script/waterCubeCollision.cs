using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class waterCubeCollision : MonoBehaviour
{
    //when a gameobject collides with the water cube,
    //apply an upward force to it, to mimic buyancy.

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
